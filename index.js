function createEmployeeRecord (name, surname, title, hourlyPay) {
    const employeeRecord = {
        firstName: name,
        familyName: surname,
        jobTitle: title,
        payPerHour: hourlyPay,
        // time in event, timeout event
    }
        return employeeRecord
}


